#include<stdio.h>
int main()
{
  int n, sum=0, c, array[10];
  printf("Enter n numbers\n");
  scanf("%d",&n);
  printf("Enter %d integers\n");
  for(c=0;c<=n;c++)
  {
    scanf("%d",&array[c]);
    sum=sum+array[c];
   }	
   
   printf("sum=%d\n",sum);
   return 0;
}
   
  
  
